-- Дневная отметка «Занят»: администратор и директор убирают мастера из очереди
-- до конца дня, не трогая уже выданные ему заявки.
--
-- Отметка живёт в строке смены, а строки смены заводятся на дату — значит
-- утром она сбрасывается сама, отдельный механизм не нужен.
--
-- Применение: .venv/bin/python migrate.py

ALTER TABLE shifts ADD COLUMN IF NOT EXISTS paused_at TIMESTAMPTZ;
